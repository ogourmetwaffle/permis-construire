create table public.dossiers (
    id bigint generated always as identity primary key,

    numero_dossier text unique not null,

    type_client text not null,
    type_projet text not null,

    nom text not null,
    prenom text not null,
    email text not null,
    telephone text,

    date_naissance date,

    adresse_client text,

    adresse_projet text not null,

    numero_parcelle text,

    surface numeric,

    description text,

    montant numeric default 0,

    mode_paiement text,

    paiement_effectue boolean default false,

    statut text default 'EN_ATTENTE_PAIEMENT',

    commentaire_admin text,

    created_at timestamp default now(),

    updated_at timestamp default now()
);


ALTER TABLE public.dossiers
ADD COLUMN date_paiement timestamp NULL,
ADD COLUMN reference_paiement text NULL,
ADD COLUMN commentaire_paiement text NULL;